var config = {
  env: 'DEV',
  offline: true,
  urls: {
    DEV: {
      network: 'url.url',
      ws: 'url.url'
    },
    TEST: {
      network: 'url.url',
      ws: 'url.url'
    },
    PROD: {
      network: 'url.url',
      ws: 'url.url'
    }
  }
}
